import React from 'react';

import { SimpleNavigator } from './SimpleNavigator';

/** The Simple presentation shell; it shares every domain provider with Standard. */
export function SimpleShell() {
  return <SimpleNavigator />;
}
